{
    "name": "Default Environment",
    "image_tests": {
        "result_dir": "${project_dir}/Tests/data/results/${branch}/${build_config}",
        "ref_dir": "${project_dir}/Tests/data/refs/${branch}/${build_config}"
    }
}
