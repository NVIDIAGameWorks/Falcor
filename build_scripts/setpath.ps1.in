# ***************************************************************
# * This script adds Falcor to the current path on Windows.
# ***************************************************************

$env:FALCOR_DIR = Get-Location
$env:PATH = $env:FALCOR_DIR + ";" + $env:PATH
$env:PYTHONPATH = $env:FALCOR_DIR + "\python" + ";" + $env:PYTHONPATH
